# If you are using mysql, add your schema to this file
# From the command line run mysql -u <USER> < mysql-schema.sql
# to create your database / columns / and some seed data
# alternatively, open mysql and copy and paste the code below
# once you've made your schema

DROP DATABASE IF EXISTS messages;

CREATE DATABASE messages;

USE messages;


CREATE TABLE messages(

);

# Seed your data with a collection of insert statements
# INSERT INTO messages() VALUES();
